#include <dandb/core/version.h>

namespace dandb {
    namespace core {
        std::string projectName() {

            return "dandb";

        }

        std::string projectVersion() {

            return "0.0.0";

        }
    }
}
